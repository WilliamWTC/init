traceroute slash16.org

# Traceroute is a network diagnostic
# tool used to track in real-time the
# pathway taken by a packet on an IP network
# from source to destination, reporting the IP addresses
# of all the routers it pinged in between.