nslookup slash16.org

# nslookup (from name server lookup)
# is a network administration command-line
# tool available in many computer operating
# systems for querying the Domain Name System
# (DNS) to obtain domain name or IP address mapping, or other DNS records.