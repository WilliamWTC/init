cat /var/run/resolv.conf # | grep "nameserver"

# This path contains the IP address of the used DNS server
# scutil --dns | grep 'nameserver\[[0-9]*\]'
# Gives list of configured DNS servers on the system